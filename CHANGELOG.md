# Changelog — shared packages

Changes to `packages/*` and `tools/*`. Each prototype has its own
`CHANGELOG.md`. Newest first. Written with `pnpm log-change root`, after
`pnpm verify` passes and the change is pushed. Each entry says *why*.
